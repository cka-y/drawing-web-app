export const SIDEBAR_WIDTH = 260;
export const DEFAULT_HEIGHT = 700;
export const MIN_SIZE = 250;
export const DEFAULT_DIMENSIONS = { width: Math.max((window.innerWidth - SIDEBAR_WIDTH) / 2, MIN_SIZE), height: DEFAULT_HEIGHT };
