export interface SelectionBox {
    width: number;
    height: number;
    top: number;
    left: number;
}

export const DEFAULT_SELECTION_STYLE: SelectionBox = {
    width: 0,
    height: 0,
    top: 0,
    left: 0,
};
